import { InputFirstCapDirective } from './input-first-cap.directive';

describe('InputFirstCapDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFirstCapDirective();
    expect(directive).toBeTruthy();
  });
});
